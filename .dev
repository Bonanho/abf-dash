MCC - gerenciar contas e compartilhar biblioteca de dados
Descobrir:
Quantas bases podemos ter ( 400 )

Criar Campanha:
- Qual objetivo da campanha
- Nome da campanha
- Inicio e Fim
- Tipo de Orçamento ( diario ou total )
- Valor 
- Inclusão de cluster:
    - Especifico
    - Exclusão
    - Observação
- Tipo de criativo ( display, video )
- Upload de criativo
- Segmentação:
    - Locais ( Pais, Estado, Cidade, Bairro )
    - Idade
    - Sexo
    - Idioma

- IA para fazer os criativos ( display e video ) com algumas premissas
- IA analisar resultado das campanhas 
- Compartilhamento de Base para terceiros ( FUTURO ) 
    - Descobrir se tem como acompanhar relatorios de bases utilizadas por terceiros

####################################################################

Tela para explicação das APIs ( Categoria e Clusters )
Gerar Invoice por campanha no final do mes

Integrações 
- GoogleAds
- Facebook

####################################################################

php artisan db:seed --class=InitialSeeder

####################################################################

Entidades
- Categorias
- Clusters
- Campanhas
- Redes

Teremos uma rede ROCK para ações de Push e MailMkt 

Api para receber resultados de venda de determinadas campanhas



### BLOCO NGINX
server {
        listen 80;

        server_name crm-dash.loc;
        root /home/bonanho/Works/crm/crm-dash/public/;
        error_log /var/log/nginx/crm-dash.loc-error.log;

        index index.html index.htm index.php;

            location / {
            try_files $uri $uri/ /index.php$is_args$args;
        }

        location ~ \.php$ {
            try_files $uri =404;
            fastcgi_pass unix:/var/run/php/php8.3-fpm.sock;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include fastcgi_params;
        }
}
